{
  "name": "@ksungz/agent-bridge",
  "version": "0.1.1",
  "description": "A subscription-native local CLI for coordinating authenticated AI coding agents through shared task context, reviews, and handoffs.",
  "type": "module",
  "bin": {
    "agent-bridge": "./dist/cli.js",
    "ab": "./dist/cli.js"
  },
  "files": [
    "dist",
    "docs",
    "examples",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "prepack": "npm run build",
    "test": "npm run build && node --test test/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "agent",
    "cli",
    "claude-code",
    "codex",
    "gemini",
    "orchestration",
    "handoff"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.19.4",
    "typescript": "^5.8.3"
  }
}
